node_modules
dist
.next
.turbo
coverage
*.log
*.tsbuildinfo
.DS_Store

# Env files - only *.example templates are committed
.env
.env.*
!.env.example
!.env.*.example

# Prisma 7 generates the client into src/, not node_modules
apps/api/src/generated

# Runtime winston log output (rotated logs + rotation audit files)
apps/api/src/storage/logs

# Local-disk upload storage (STORAGE_DRIVER=local) - dev/self-hosted only
apps/api/uploads

# Next.js auto-generates these (next dev/build regenerate them if missing).
# Kept local-only rather than committed.
apps/web/AGENTS.md
apps/web/CLAUDE.md

# TanStack Router codegen (regenerated by the router-plugin on dev/build)
apps/admin/src/routeTree.gen.ts
