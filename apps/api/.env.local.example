NODE_ENV=######
PORT=######
DATABASE_URL=######

JWT_SECRET=######
JWT_ACCESS_TTL=######
JWT_REFRESH_TTL=######

PASSWORD_BREACH_CHECK_ENABLED=######

CAPTCHA_ENABLED=######

OAUTH_REDIRECT_BASE_URL=######

GMAIL_USER=######

FRONTEND_URL=######

ADMIN_URL=######
ALLOWED_ORIGINS=######

STORAGE_DRIVER=######
UPLOADS_DIR=######
API_PUBLIC_URL=######

TENANT_BASE_DOMAIN=######

REDIS_URL=######

# @outfiqe/image-pipeline — all optional, sensible local-Docker defaults if unset.
# See packages/image-pipeline/README.md for what each one tunes.
IMAGE_QUEUE_REDIS_HOST=######
IMAGE_QUEUE_REDIS_PORT=######
IMAGE_QUEUE_REDIS_PASSWORD=######
IMAGE_QUEUE_REDIS_TLS=######
IMAGE_STORAGE_ROOT_DIR=######
IMAGE_TEMP_UPLOAD_DIR=######

# AES-256-GCM key encrypting BankAccount.accountNumber at rest — has a dev-only
# default, but production MUST set a real 64-hex-char key: openssl rand -hex 32
BANK_ACCOUNT_ENCRYPTION_KEY=######
