# Copy to .env and adjust. Never commit .env itself.

# The API origin this admin panel talks to.
VITE_API_URL=http://localhost:4000

# The apps/web origin. There's no login form in this app — signing in
# happens on apps/web, which sends ADMIN-role users straight back here.
VITE_WEB_URL=http://localhost:3000
